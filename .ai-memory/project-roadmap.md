# 当前阶段与后续方向

## 当前状态

当前仓库已经从“基础模板可运行”推进到“平台底座可用、平台能力层部分落地、后端业务实验层已基本收口”的阶段。

基于当前代码和已提交 git 历史，可以直接确认：

- 参数设置、多角色、字典管理、后台标准页母版、表单构建桥接、代码生成承接已经进入已提交主线。
- `go-admin/app/platform` 下的 `workflow / module_registry / attachment` 及前端 `我的待办 / 我发起的 / 流程详情抽屉` 已进入已提交历史。
- clone 基线本身已经包含同事融合进来的业务代码，不应被描述成“回到纯空脚手架”。
- 本地叠加的后端业务实验层已基本收口，当前 go-admin 已回到 clone 基线附近。
- finance 本地扩展不再作为当前主线继续推进；feishu / kingdee / biz_action_log / `sys_user` 扩展类本地后端实验也按同一口径收口。
- 当前 working tree 剩余的主要是前端原型、样式、文档和脚手架增强相关改动。

## 当前待收口的部分

- `统一业务操作日志最小规范` 仍未形成完整已提交真相源。
- 头像相关功能与文档仍混有本地未提交状态，不能写成仓库主线能力。

## 当前最值得继续的方向

1. 先收口文档和真相源，把“已提交状态”和“本地 working tree 状态”彻底分开。
2. 再做前端原型补全、页面交互完善和脚手架增强，不新增一轮本地后端业务扩散。
3. 默认围绕已提交后端基线做真实页面承接，不把本地后端实验层重新带回主线。

## 当前不建议优先做的

- 重新在本地扩写 finance / feishu / kingdee / biz_action_log / `sys_user` 扩展类后端业务线
- 把数据库菜单真相搬回前端硬编码
- 把本地未提交状态直接写成官方完成度
- 在既有平台能力之外再开一条新的重型业务后端线

## 验收基线

当前文档与本地联调收尾仍以这些检查为准：

- `./scripts/check-local.sh`
- `go test ./...`
- `pnpm typecheck`
- `pnpm build:local`
- `pnpm test:e2e`
